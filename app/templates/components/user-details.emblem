.sub-container
  .columns
    .column.content-right
      h4
        | Start Date: 27th August 2016
  table
    thead
      tr
        th
          | NAME
        th
          | USERNAME
        th
          | EMAIL
        th
          | PASSWORD
        th
          | STATUS
        th
          | EDIT
      tr
        td
          p class=showHide:show:hide
            = user.fullName
          p class=editUnedit:edit:un-edit
            = input class="input" placeholder=user.fullName value=user.fullName
        td
          p class=showHide:show:hide
            = user.username
          p class=editUnedit:edit:un-edit
            = input class="input" placeholder=user.username value=user.username
        td
          p class=showHide:show:hide
            = user.email
          p class=editUnedit:edit:un-edit
            = input class="input" placeholder=user.email value=user.email
        td
          p class=showHide:show:hide
            = user.password
          p class=editUnedit:edit:un-edit
            = input class="input" placeholder=user.password value=user.password
        td
          | active
        td
          p class=showHide:show:hide click="editText"
            = fa-icon "pencil"

          p class=editUnedit:edit:un-edit click="saveText"
            = fa-icon "check"

          p.margin-left class=editUnedit:edit:un-edit click="saveText"
            = fa-icon "times"
  .columns
    .column.is-half
      .tabs.is-boxed
        ul
          li class=overviewClass
            a click="displayOverview"
              | OVERVIEW

          li class=namespaceClass
            a click="displayNamespace"
              | NAMESPACE

          li class=subscriptionClass
            a click="displaySubscription"
              | SUBSCRIPTION
  if isOverview
    .columns
      .column.box.is-one-quarter.margin-top10
        = fa-icon "user"
        div
          | Projects Added
        div
          | 140

  else if isNamespace
    .columns
     .column.box.is-one-third.margin-top10
        div
          form.namespace-form
            p.control
              = input class="form-control" type="checkbox"
              | &nbsp; Any Namespace
            p.control.is-fullwidth
              = textarea class="form-control input" placeholder=user.namespaces type="text" value=user.namespaces
            button click="addNamespaces"
              | ADD NAMESPACES
  else
    if addSubscription
      .columns
       .column.box.is-one-quarter.margin-top10
        div click="openSubscriptionForm"
          = fa-icon "trash-o"
        div
          | ADD SUBSCRIPTION
    else
      .columns
        .column.is-half
          .columns
            .column.is-half
              .box
                | Pricing
                span
                  select.input
                    each pricings as |pricing|
                      option value=pricing.name
                        = pricing.name
            .column.is-half
              .box
                | Coupon
                select.input
                  each coupons as |coupon|
                    option value=coupon.code
                      = coupon.code
          .columns
            .column
              .box
                | Source
                select.input
                  each sources as |source|
                    option value=pricing.name
                      = pricing.name
        .column.is-half
          .columns
            .column.is-half
              .box
                h5
                  | PAY PER SCAN
            .column.is-half
              .box
                h5
                  | PAY PER MONTH
          .columns
            .column.is-half
              .box
                | Scans Left
                br
                = input type=number
            .column.is-half
              .box
                | Expiry Date
                = input type=date

      .columns
        .column
          button click="closeSubscriptionForm"
            | cancel
          button
            | update
